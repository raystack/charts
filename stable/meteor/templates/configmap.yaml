apiVersion: v1
kind: ConfigMap
metadata:
  name: "{{ include "meteor.name" . }}-{{ .Values.jobName }}-configmap"
  namespace: {{ .Release.Namespace }}
data:
{{- $files := .Files }}
{{- range .Values.recipes }}
{{ .key | indent 2 }}: |-
{{ $files.Get .localPath | indent 4}}
{{- end }}
